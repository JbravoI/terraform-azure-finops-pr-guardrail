# Scripts

Plan parsing, policy evaluation, cost normalisation, and PR-comment rendering helpers will live here. Scripts must keep output deterministic for fixture-based tests and must redact sensitive values.
